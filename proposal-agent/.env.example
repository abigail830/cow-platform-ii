# --- Azure OpenAI (recommended if you use Azure) ---
AZURE_API_KEY=
AZURE_API_BASE=https://YOUR_RESOURCE.cognitiveservices.azure.com
# Do NOT append /openai — the SDK adds deployment paths itself.
AZURE_API_VERSION=2024-08-01-preview
AZURE_DEPLOYMENT_NAME=gpt-4o

# Azure OpenAI (recommended if you use Azure)

# --- Anthropic (alternative) ---
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# --- OpenAI direct (alternative) ---
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o

# Optional: --provider azure|anthropic|openai  and  --model <deployment-or-model>
