[project]
name = "proposal-agent"
version = "0.1.0"
description = "Streaming OKF consumer agent for smart-proposal-knowledge"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "openai>=1.40.0",
  "anthropic>=0.40.0",
  "pyyaml>=6",
  "python-dotenv>=1.0.0",
]

[project.scripts]
proposal-agent = "stream_cli:main"
