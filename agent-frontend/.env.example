# sse (default) or long-poll if corporate proxy blocks SSE
VITE_FLUE_LIVE_MODE=sse

# Vercel only (middleware.ts): backend project origin, no trailing slash
# BACKEND_ORIGIN=https://cow-platform-ii.vercel.app
