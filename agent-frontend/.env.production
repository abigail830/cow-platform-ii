# `npm run build` / Vercel — browser calls backend directly (no Edge proxy).
# long-poll: Vercel serverless cannot fan in-process SSE across function instances.
VITE_FLUE_LIVE_MODE=long-poll
VITE_API_ORIGIN=https://cow-platform-ii.vercel.app
